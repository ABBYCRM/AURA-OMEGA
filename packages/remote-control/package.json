{
  "name": "@workspace/remote-control",
  "version": "0.0.1",
  "private": true,
  "description": "BOS-OMEGA remote-control orchestrator — runs in api-server, dispatches commands to PC Agents, manages device registry, exposes /api/devices/* routes.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./adapters": "./src/adapters/index.ts",
    "./routes": "./src/routes.ts"
  },
  "dependencies": {
    "@workspace/db": "workspace:*",
    "@workspace/pc-agent": "workspace:*",
    "drizzle-orm": "catalog:",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "catalog:",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}